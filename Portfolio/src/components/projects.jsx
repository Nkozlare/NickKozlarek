import React, { useState, useEffect } from 'react';
import styled from 'styled-components';
import { StyledFlex } from '../styledComponents.jsx';

const StyleProjects = styled(StyledFlex)`
    
`

export default function template () {
    return (
        <div>

        </div>
    )
}